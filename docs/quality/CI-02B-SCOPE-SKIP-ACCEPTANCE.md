# CI-02B scope-skip acceptance fixture

This temporary docs-only change exists to exercise the PR workflow's non-Rust
scope path. It is intentionally not intended for merge and will be removed
with the short-lived acceptance pull request after hosted evidence is captured.
