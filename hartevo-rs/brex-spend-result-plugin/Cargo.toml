[package]
name = "hartevo-brex-spend-result-plugin"
description = "Standalone Layer-1 governed Brex spend, limit, and policy observation result plugin"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "Proprietary"
authors = ["Hartevo"]
repository = "https://github.com/tangpingqingwa/hartevo-desktop"
publish = false

# This crate is intentionally a standalone Layer-1 root. Host wiring,
# native Brex HTTPS, and kernel authority remain outside Issue #711's paths.
[workspace]
resolver = "3"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
thiserror = "2.0"
zeroize = { version = "1.8", features = ["derive"] }

[dev-dependencies]
proptest = "1.11"

[lints.rust]
unsafe_code = "forbid"
missing_debug_implementations = "warn"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"
doc_markdown = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
result_large_err = "allow"
struct_excessive_bools = "allow"
too_many_arguments = "allow"
too_many_lines = "allow"
similar_names = "allow"
needless_pass_by_value = "allow"
cast_possible_truncation = "allow"
cast_sign_loss = "allow"
if_not_else = "allow"
match_same_arms = "allow"
